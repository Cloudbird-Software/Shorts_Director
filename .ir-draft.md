标题：IR-NNNN 重构为生成一等公民的营销视频实验平台（V100 实测认知）

## job（要做什么）

把 Shorts_Director 从「实拍素材治理型生产系统」整体重构为「AI 生成一等公民的营销视频实验平台」：

1. 退役与生成范式不符的模块（slotquery 谓词检索、planner 时序求解、brandkernel 苏格拉底 onboarding、shot 实体治理、compat 前向兼容层及其关联 schema/测试/词表）。
2. 新建实验骨架：GPU 环境探测（shorts-doctor）、生成算子（operators/：gen_i2v、gen_tts、gen_lipsync、vlm_boolean，走 C2 契约）、评估编排（internal/eval + shorts-eval：套件执行 → 断言判定 → 内容寻址 run artifact → 报告）。
3. 以数个 mock 商家（公开/合成数据集）为场景，实测 6 种镜头制式的抽卡通过率，并组合出满足营销要求的完整视频（信息层叠加零失败）。

## 触发场景

owner 提出范式反转意图（飞书文档《AI 原生营销视频生产系统 — 业务认知与架构需求文档》v1.1，2026-09-01）：不以实拍视频标准要求 AI，而以 AI 稳定生成能力为一等公民反推视频形态；确定性信息（文字/价格/地址/电话/Logo）模板叠加实现零失败。当前持有一块 V100-32G（月租含电价）作为免费实验资源；后续 A100 2×40GB 到位后一次性完成对照实验。

## 痛点证据

- 现仓库 12 项 Freeze Gate 仅 1 项达成（README 看板），契约冻结路线在「单 agent + 单卡验证概念」场景下过重，且骨架重心（素材库治理/谓词检索/月度排期）与新意图错位。
- 文档的出片率数字（形态1 >85%、形态4 >90% 等）标注为「目标基准，需在 MVP 阶段通过实际跑测校准」——当前仓库没有任何工具能产出这三个数字（出片率/日产能/单条成本）。
- 文档技术路线假设 A100 2×40GB；V100（sm_70，无 bf16/FlashAttention-2）上哪些开源 I2V/TTS/lipsync 模型可用，本身就是待验证命题，无现成结论。

## 期望可观察变化（验收对账基准）

1. `make doctor` 在 V100 机器上输出机器可读的 capability profile（GPU 算力/显存/驱动/CUDA/ffmpeg/docker/候选模型逐项 feasible|infeasible+原因），落为内容寻址 artifact。
2. `make eval-suite SUITE=<形态>` 对指定镜头制式执行 K 次抽卡，产出内容寻址 run artifact：每条生成物 pass/fail + 证据 URI + 聚合出片率（P(≥1 条可用)）。
3. 数个（≥3）mock 商家场景下，形态1（I2V 呼吸感展示）与形态4（数字人口播）各组合出 ≥1 条完整营销视频 mp4：含信息层叠加（店名/价格/地址/电话）+ AIGC 标识，且通过对应 QC 断言包。
4. 仓库 README 的 Freeze Gate 看板替换为「假设看板」：每条实验假设（E0–E7）登记状态/证据链接/结论，结论可回查 run artifact。
5. 退役模块从代码库消失：slotquery/planner/brandkernel/entity/compat 及其 schema/testdata/tests 不再存在，`make check` 恒绿。

## 非目标

- 不做商家输入端（小程序/表单）、不做 DB/队列/对象存储/编排调度平台、不做多租户。
- 不做 6 形态全覆盖：只实测形态1 与形态4（光谱两端），其余 4 形态留待数据配置。
- 不做 BAML 打标/访谈运行时（TagShot/Interview 服务于旧意图的素材治理）。
- 不在 V100 阶段优化产能/成本到文档目标（日 60–100 条、≤5 元/条）——本阶段只建立测量仪器并产出第一批实测数字；A100 对照实验一次性完成。
- 不引入 Remotion/OTIO/微服务。

## 约束

- 语言栈与纪律不变：Go 控制面、Python 算子（C2 契约 stdin/stdout JSON）、TS 渲染层；一 PR 一事 diff<400 行；`make check` 恒绿；不改 .github/workflows/** 与 Makefile check 目标。
- Python 侧新依赖（torch/diffusers/CosyVoice/wav2lip 等）仅限算子镜像内，入仓前按硬规则 3 报批（名称/用途/许可证/替代性）；禁 AGPL/GPL-3.0/SSPL。Go/TS 侧零新增依赖。
- 实验可复现性沿用 A2 公理：一切非确定性（模型推理/seed）显式落盘为内容寻址 artifact；评估判定题化沿用 A5 公理：断言→bool+证据，禁止打分做门禁。
- 数据来源只用公开数据集或合成数据（如 Unsplash/Pexels/LFW 类公开图库 + 合成信息表），不使用真实商家个人信息。

## 验收证据

- E0 capability profile artifact + 逐模型可行性矩阵（doctor 输出）。
- E1 单发冒烟 run artifacts：每个可行模型一条 5–8s I2V 片段 + latency/peak_mem。
- E2/E3 出片率报告：形态1×模型×seed[1..5]、形态4 全链路，判定明细 + 聚合。
- E5 端到端切片：≥3 个 mock 商家 × 2 形态的完整 mp4 + QCReport + 全链耗时分解。
- E6 裁判校准：vlm_boolean vs 人工标注（~100 条）一致率混淆矩阵。
- 假设看板（README）全部条目带证据链接。

## 可逆性偏好

退役模块采用「先抓 golden 再删」（R-06 纪律）：退役 PR 前将既有测试/fixture 归档到一次性 tag，允许从 tag 恢复；新模块全部为新增目录/文件，回滚=删目录。

## 质量速度旋钮

- 速度优先：spec 后快速开卡、小步 PR；弱模型可做的机械迁移（删除类）允许 fan-out。
- 质量底线不放松：C2/QC/golden 契约测试全绿；eval 报告数字必须可由 run artifact 复算。
